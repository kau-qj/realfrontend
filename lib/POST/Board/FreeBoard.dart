import 'package:flutter/material.dart';

class FreeBoard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: Free Board 화면을 구성하는 코드 작성
    return Container(
      child: Center(
        child: Text('자유 게시판 '),
      ),
    );
  }
}
